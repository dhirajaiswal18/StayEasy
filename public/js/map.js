
	// mapboxgl.accessToken = mapToken;

  //     const map = new mapboxgl.Map({
  //       container: "map", // ID of the div where the map will render
  //       // style: "mapbox://styles/mapbox/streets-v11", // Mapbox style
  //       center: listing.geometry.coordinates, // Coordinates [longitude, latitude]
  //       zoom: 9, // Initial zoom level
  //     });
      
  //     // Add a marker at the listing's location
  //     new mapboxgl.Marker( {color:"red"} )
  //       .setLngLat(listing.geometry.coordinates) // Coordinates [longitude, latitude]
  //       .setPopup(
  //         new mapboxgl.Popup({ offset: 25 }) // Optional popup
  //           .setHTML(`<h5>${listing.title}</h5><p>you'll be living here!</p>`)
  //       )
  //       .addTo(map);






    // const marker = new mapboxgl.Marker({ color: "red" })
    // .setLngLat(listing.geometry.coordinates) // Listing.geometry.coordinates
    // .setPopup(
    //   new mapboxgl.Popup({ offset: 25 }).setHTML(
    //     `<h4>${listing.title}</h4><p>Exact Location will be provided after booking</p>`
    //   )
    // )
    // .addTo(map);
  



   
